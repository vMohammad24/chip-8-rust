[package]
name = "chip-8"
version = "0.1.0"
edition = "2024"

[dependencies]
minifb = { version = "0.28" }
rand = "0.10.1"
rfd = "0.17.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"
